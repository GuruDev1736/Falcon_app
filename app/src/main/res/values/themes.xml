<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Falcon" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="colorPrimaryDark">@color/blue_dark</item>
        <item name="colorAccent">@color/blue_light</item>
        <item name="android:statusBarColor">@color/blue_primary</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.Falcon" parent="Base.Theme.Falcon" />

    <style name="Theme.Falcon.Splash" parent="Theme.Falcon">
        <item name="android:windowBackground">@color/blue_primary</item>
        <item name="android:statusBarColor">@color/blue_primary</item>
    </style>
</resources>